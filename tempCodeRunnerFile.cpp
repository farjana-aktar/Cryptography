    // // Ensure that the key is long enough
    // if (key.length() < text.length()) {
    //     cout << "Error: Key length must be greater than or equal to text length." << endl;
    //     return 1;  // Exit if the key is not long enough
    // }